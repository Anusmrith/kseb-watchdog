{
  "name": "kseb-slab-watchdog",
  "version": "1.0.0",
  "description": "⚡ KSEB Slab Watchdog (കരന്റ് വാച്ച്ഡോഗ്) - Kerala electricity meter tracker, burn rate predictor, slab cliff radar and appliance inspector",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "vite build && gh-pages -d dist",
    "preview": "vite preview",
    "test": "node test/verify.js"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^6.2.0"
  }
}
